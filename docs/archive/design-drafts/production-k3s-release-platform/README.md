# Production K3s Release Platform 草稿归档

本目录保存一组历史 Draft 设计文档，用于记录当时对目标平台的产品、架构和实施设想。

这些文件保留原始上下文，不再和当前 KADM 操作文档并列，也不保证与当前仓库结构一致。

适用方式：

- 作为历史背景阅读
- 用于回看当时的设计思路
- 不作为当前安装、发布、运维入口

---

# Production K3s Release Platform Draft Archive

This directory preserves a historical set of draft design documents for the target platform's product, architecture, and implementation ideas.

The files are kept in their original context. They are no longer part of the active KADM workflow and may not match the current repository layout.

Use them only as:

- historical background
- a record of previous design thinking
- not the current install, release, or operations entrypoint
